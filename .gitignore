node_modules/
.vercel/
.env*
.DS_Store

# Build artefacts — генерируются scripts/build-i18n.mjs из index.html
en.html
kz.html

# Archive: previous design version (kept locally for rollback, not for deploy)
_archive/

# Claude Design source bundle (kept locally as reference, not for deploy)
SITS-2/
SITS-2.zip

# Hero video source (heavy HEVC .MOV — web-оптимизированные варианты лежат в brand/hero/)
Послание Президента/
